package hashTable;

public class Main {
    public static void main(String[] args) {
        //Example usage

        /*
        HashTable hashTable = new HashTable();
        hashTable.set("name", 1);
        hashTable.set("age", 2);
        hashTable.set("city", 3);

        hashTable.printTable(); // Displays the hash table contents.
        System.out.println(hashTable.get("name")); // Output: 1
        System.out.println(hashTable.keys()); // Output: [name, age, city]

        */
    }
}
